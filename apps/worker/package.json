{
  "name": "@aap/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "start": "tsx src/main.ts",
    "build": "echo \"worker runs from source via tsx (no build step)\"",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@aap/core": "workspace:*",
    "@aap/db": "workspace:*",
    "@electric-sql/pglite": "^0.5.6",
    "dotenv": "^16.5.0",
    "kysely": "^0.28.2",
    "pino": "^9.7.0",
    "tsx": "^4.19.4"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
